
https://oz.warp.dev/welcome/environment

https://docs.warp.dev/reference/api-and-sdk

Okay, so the idea would be for us to have this setup that helps developers to publish their work to our Oz managed agents herd management, I'm not sure how we call it.

And for that we need to build some demo playgrounds where we would have Codex environment, Claude environment and in which environment we should have some agentic tasks being executed within each of those environments.

and that it's all setup, verified that it's both working within local setup with Claude and with Codex, then it works easily with OZ, we have some skills and ways how to publish it, and then we verify that OZ is working as well.

importantly we have to definitely check it on warp hosted environment. We will later be using our self-hosted but not now. It's to do. And locally we just check that local agents are working which means Codex or Claude themselves are doing their tasks on the playground. I also would definitely be expecting some of run artifacts to be present so that we will be able to then check how runs went, did they go well or not. We should be having that observability layer for sure because it will define and help us to get the results of the run.
